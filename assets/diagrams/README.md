# Diagram Assets

Keep editable diagram sources and rendered exports in this directory. Prefer Mermaid in the relevant Markdown page when an external image is unnecessary.

Use descriptive filenames and record which documentation page owns the diagram. Review diagrams for sensitive environment details before publication.

This directory is not automatically exported by the Wiki build. Asset publication must be added explicitly when a page needs it.
